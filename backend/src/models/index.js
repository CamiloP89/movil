const User = require('./User')
const Category = require('./Category')
const Subcategory = require('./Subcategory')
const Product = require('./product')

// Permite importar los dodelos de forma sencilla

module.exports = {
    User,
    Category,
    Subcategory,
    Product
};